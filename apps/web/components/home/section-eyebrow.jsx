/** Reusable section eyebrow — pill label pattern (Brand Identity Book). */
export function SectionEyebrow({ children, className = "" }) {
  return <p className={`phx-eyebrow ${className}`}>{children}</p>;
}
