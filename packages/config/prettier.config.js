/**
 * Shared Prettier preset — consumed by the repo root and every workspace,
 * so formatting is a property of the platform, not of any one app.
 */
export default {
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 80,
  tabWidth: 2,
};
